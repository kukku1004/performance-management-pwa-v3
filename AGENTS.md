# AGENTS.md — 팀 성과·성장관리 PWA V3

## 1. 버전 보호

- `main`은 기존 운영 버전이다. 수정하지 않는다.
- Claude V2 브랜치는 수정하지 않는다.
- Codex 작업은 반드시 `codex/performance-management-pwa-v3`에서만 한다.
- 기존 `/preview/`, Claude `/preview-v2/`를 유지한다.
- Codex V3는 `/preview-v3/`로 별도 배포한다.

## 2. 개발 원칙

- 새 프로젝트로 다시 만들지 않는다.
- `main`의 기존 기능을 기반으로 개선한다.
- 정상 작동하는 기능과 계산 로직은 최대한 유지한다.
- 필요한 파일만 수정한다.
- 관련 없는 코드 리팩터링 금지.
- 같은 데이터를 여러 화면에서 다시 입력시키지 않는다.
- 자동 계산 가능한 값은 자동 처리한다.

## 3. 앱의 두 핵심 업무

### 성과평가

과제/팀원 준비
→ 평가
→ 결과 확인
→ 고과 확정
→ Excel 다운로드
→ 팀원별 PDF 전달

### 팀원 관리

팀원 선택
→ 성과 확인
→ 최근 고과 확인
→ 성장/승진 시뮬레이션
→ 면담 준비
→ 면담 일정
→ 면담 기록

두 메뉴는 분리하되 데이터는 자동 연결한다.

## 4. 성과평가

기존 평가 계산 로직을 임의 변경하지 않는다.

유지:

- 과제
- 팀원
- 중요도
- 과제 성과
- 과제 비중
- 기여도
- 개인 수행등급
- 평가기준
- 개인별 성과 계산

개인 수행등급을 미사용해도 기여도 입력은 가능해야 한다.

## 5. 결과 / Excel / PDF

### 결과

테이블 중심으로 심플하게 구성한다.

핵심:

- 팀원
- 직급
- 성과점수
- 최종 고과
- 전년도
- 변화
- 상태

### Excel

`.xlsx`로 생성한다.

기본 시트:

1. `01_팀원 성과결과`
2. `02_개인별 상세`
3. `03_과제별 결과`
4. `04_평가기준`

단순 데이터 dump가 아니라 보고·보관 가능한 문서로 만든다.

### 개인 PDF

- 미리보기
- 개인 PDF
- 복수 선택
- 전체 다운로드

개인 PDF에는 해당 팀원의 정보만 포함한다.
다른 팀원의 이름, 점수, 등급, 순위는 노출하지 않는다.

가능하면 1페이지, 최대 2페이지로 만든다.

## 6. 팀원 관리

1920px Desktop 기준 기존 구조를 유지한다.

- 좌측: 팀원 목록
- 상단: 팀원 요약
- 하단 왼쪽: 성과·성장 시뮬레이션
- 하단 오른쪽: 면담
- 우측: 면담 일정 캘린더

기존 splitter와 캘린더 최소/최대 기능을 유지한다.

### 팀원 요약

- 이름
- 현재 연도 성과
- 최근 5년 고과
- 승진심사 시기
- 승진 가능 여부

`현재 성과` 대신 실제 연도를 표시한다.
예: `2026 성과`

승진 가능 여부는 중복 표시하지 않는다.

## 7. 성장/승진

기존 기능을 유지한다.

- accordion 접기/펼치기
- 등급 보기
- 점수 보기
- 평가 이력
- 승진 시뮬레이션
- 필요한 점수

접힌 상태에서도 핵심 결과는 보여준다.

예:

- `승진 가능`
- `승진까지 3점 필요`

## 8. 면담

면담은 핵심 기능이다. 삭제하거나 축소하지 않는다.

명칭:
`면담 포인트`

유지:

- 면담일
- 주요 논의
- 팀원 의견
- 팀장 의견
- 후속 확인사항
- 과거 면담 기록
- 면담 일정

`액션`이라는 모호한 항목은 만들지 않는다.

## 9. 디자인

최대한 심플한 업무용 UI로 만든다.

기본:

- White
- Light Gray
- Dark Gray / Black

포인트:

- Orange 1종

Orange 사용:

- 주요 CTA
- 선택 상태
- 핵심 결과
- Active / Focus

금지:

- 큰 Orange 배경
- Gradient
- 과도한 Shadow
- Glassmorphism
- Illustration
- Card 남용
- Card inside Card

Badge는 공통 컴포넌트 하나로 통일한다.

Icon은 가능하면 Lucide 같은 단순한 outline library 하나만 사용한다.

## 10. PWA

V3도 PWA로 유지한다.

- manifest
- installable
- standalone
- service worker
- app icon
- responsive desktop
- 기본 offline shell

## 11. 버전 비교

다음 세 버전을 독립적으로 유지한다.

- 기존: `/preview/`
- Claude V2: `/preview-v2/`
- Codex V3: `/preview-v3/`

V3에서 세 버전을 새 탭으로 열어 비교할 수 있게 한다.

기존이나 Claude 브랜치를 수정해서 비교 기능을 추가하지 않는다.

## 12. 절대 금지

- `main` 수정
- Claude V2 수정
- 기존 URL 변경
- 기존 기능 삭제
- 캘린더 삭제
- splitter 삭제
- 성장/승진 삭제
- 면담 삭제
- 데이터 중복 입력
- 승진 정보 중복 표시
- 불필요한 대시보드/KPI/차트 추가
- 관련 없는 파일 수정
- 전체 앱 재작성

## GUI / Component Consistency

기존 버전보다 전체 GUI를 더 심플하고 깔끔하게 정리한다.

목표는 새 기능을 많이 넣는 것이 아니라,
**전체 화면이 하나의 일관된 디자인 시스템으로 보이게 만드는 것**이다.

### 공통 원칙

- 화면마다 다른 스타일을 임의로 만들지 않는다.
- 같은 역할의 UI는 같은 스타일을 사용한다.
- 시각적으로 복잡해 보이는 요소를 줄인다.
- 정보 구조는 색보다 여백, 타이포, divider로 구분한다.
- 업무용 시스템답게 차분하고 정돈된 인상을 우선한다.

### 반드시 통일할 대상

- Button
- Input
- Select
- Table
- Badge
- Tabs
- Card / Panel
- Section Header
- Modal / Dialog
- Empty State
- Tooltip
- Icon Button

### Button 규칙

- Primary / Secondary / Ghost 정도의 최소 체계만 사용한다.
- 버튼 높이, radius, font size, padding을 통일한다.
- 페이지마다 다른 버튼 스타일을 만들지 않는다.

### Input / Select 규칙

- 입력창 높이, border, radius, padding, focus style을 통일한다.
- 검색창과 일반 입력창 스타일이 지나치게 다르지 않게 한다.

### Table 규칙

- 모든 테이블은 공통 header style, row height, cell padding, border 규칙을 사용한다.
- 숫자/텍스트 정렬 규칙을 통일한다.
- 테이블 밀도는 지나치게 빽빽하지 않게 정리한다.

### Badge 규칙

- 모든 상태 badge는 하나의 공통 Badge 컴포넌트만 사용한다.
- 높이, radius, padding, font, border 규칙을 완전히 통일한다.
- 상태에 따라 색상만 다르게 한다.

### Card / Panel 규칙

- Card 남용 금지.
- 정말 필요한 섹션만 card/panel로 사용한다.
- 카드의 border, radius, padding, shadow를 통일한다.
- Card inside Card 구조를 피한다.

### Section Header 규칙

- 섹션 제목, 설명 문구, 우측 액션 버튼 배치를 공통 패턴으로 통일한다.
- 페이지마다 헤더 스타일이 달라지지 않게 한다.

### Spacing 규칙

- margin / padding 값을 일정한 scale로 통일한다.
- 페이지마다 여백 밀도가 크게 다르지 않게 한다.

### Typography 규칙

- 제목 / 섹션 제목 / 본문 / 보조텍스트 정도의 단계만 사용한다.
- 화면마다 글자 크기 체계가 달라지지 않게 한다.

### 최종 목표

기존 버전보다 더 심플하고 깔끔하게 보이되,
새로운 시각 요소를 추가하는 방식이 아니라
**중복 스타일 제거 + 공통 컴포넌트화 + 여백/타이포 정리**
방식으로 개선한다.
